<!--
Template for new slash commands. Copy this to `.claude/commands/<name>.md`
when you create a new skill that's worth invoking by command.

A slash command is a thin trigger. It points at the skill's SKILL.md and
hands off. Don't put process logic in the command file — that lives in the
skill.
-->
Read `skills/<skill-name>/SKILL.md` and follow the process defined there.

<Optional: any modifiers, defaults, or context this command should add.
For example: "Default to brief output unless the user specifies otherwise.">
